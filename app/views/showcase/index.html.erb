<% @products.each do |p| %>
  <div class="col-lg-3">
    <%= image_tag  p.image %>
    <h4 class="text-center"><%= p.title %></h4>
    <p class="text-center">
      <button class="btn btn-primary">R$ <%= format_price p.price %></button>
    </p>
  </div>
<% end %>
